# Page Pulse configuration

# The port the server listens on (default: 4000)
PORT=4000

# Timeout in milliseconds for page fetches (default: 8000)
TIMEOUT_MS=8000

# Path to the built frontend directory (relative to project root)
# Only used in production when serving the React app from Express.
FRONTEND_DIST=project/dist
